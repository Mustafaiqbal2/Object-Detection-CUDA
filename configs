# default.yaml

model:
  path: "data/models/pretrained_model.h5"
  confidence_threshold: 0.5

tracking:
  max_lost_frames: 5
  tracking_algorithm: "SORT"  # Options: SORT, DeepSORT, etc.

webcam:
  width: 640
  height: 480
  fps: 30

performance:
  enable_monitoring: true
  log_interval: 10  # seconds

output:
  save_video: true
  output_path: "output/tracked_video.avi"